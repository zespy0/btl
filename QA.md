# 검증 기록 · 2026-09-13

## 디자인 리뉴얼 검증 (레트로 코믹 톤 + 마스코트 애셋)

- TypeScript 검사 및 Vite production build 통과.
- 1440×900에서 홈·후보지 지도·상세·공간 모델링·30일 실증·관리자·방문자·QR 설문 화면 렌더링 확인.
- 히어로 내 Leaflet 지도 타일 로드 확인(높이 250px, 타일 요청 정상).
- 가로 넘침 없음: 1440px 뷰포트에서 문서 폭 1425px, 390px 뷰포트에서 문서 폭 375px.
- 마스코트·역할 아이콘·조건 픽토그램이 각 화면에서 의도한 위치에 표시됨.
- 예시 응답 시드 후 대시보드 지표(83건 / 152명 / 3.5 / 70%)와 일자별·항목별 차트 정상 표시.

### 리뉴얼에서 수정한 문제

- 티커의 음수 마진이 가로 스크롤을 만들던 문제를 폭 계산과 컨테이너 overflow-x 처리로 해소.
- 히어로 지도가 높이를 얻지 못해 비어 보이던 문제를 최소 높이 지정으로 해소.
- 후보지 카드 상단 마스코트가 경계 밖으로 잘리던 문제를 overflow 처리와 위치 조정으로 해소.

### 리뉴얼에서 검증하지 않은 범위

- 색 대비(WCAG) 수치 측정과 스크린리더 전문 감사.
- 실제 인쇄 출력물의 시각 확인(인쇄 스타일은 그림자·티커 제거로만 처리).

## 이전 검증 (역할·지도·AI 기능 추가 후)

## 통과한 항목

- TypeScript 검사 및 Vite production build 통과.
- 지도 핀 선택과 카드 목록 연동, 검색·동네·데이터 구분·확인된 곳만 필터 동작 확인.
- 조망 측정: 키보드로 방위 조정 시 192°(남), 바다 방향 일치도 56%→66% 갱신 및 '사용자가 조정한 값' 안내 전환 확인.
- 내장 시연 모드 배치안 생성: 48㎡ 기준 사용 면적 30㎡, 동시 체류 12명 계산과 개념 배치도 표시 확인.
- Gemini 연동 모드: 키 미입력 시 생성 버튼 비활성화와 안내 문구 확인.
- 관리자: 후보지 대장 집계, 공개 상태에서 권리·안전 미확인 후보지의 공개 토글 비활성화, 문의 상태 '접수→담당 배정' 전환 확인.
- 방문자: 공개 공간 선택, 운영 정보 표시, 관심 표시 후 집계 안내 확인.
- 준비 순서: 후보지 조건을 반영한 초안 생성, 저장 후 새로고침 복원 확인.
- 390×844 모바일 뷰포트에서 콘텐츠 폭 375px와 scrollWidth 375px 일치(가로 넘침 없음).

## 수정한 문제

- 조건 판정이 '확인 필요'까지 보류로 처리해 시연 공간이 미확인으로 표시되던 문제를 고쳐, 미확인만 보류로 보고 확인 필요는 별도 문구로 표시.
- 배치 개념도의 텍스트·동선 겹침 정리.
- 지도 라벨 겹침을 좌표 조정과 외곽선 처리로 해소.

## 검증하지 않은 범위

- 실제 Gemini API 키로의 호출 결과(키를 사용하지 않았으므로 응답 파싱 경로는 미실행).
- OS 인쇄/PDF 저장 대화상자.
- 전체 키보드 순서 및 스크린리더 전문 감사.
- 공공 API, 실제 지도 타일, 소유자 동의·허가·제휴, 30일 운영 실적은 연결하지 않았습니다.
